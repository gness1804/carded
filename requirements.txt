# Carded - Production Dependencies
# Generated from pyproject.toml
# Note: baml-py must be pinned to an exact version.
# BAML enforces an exact match between the runtime and the generator that
# produced baml_client/ — >= will break in Docker/CI builds.

# Web framework
fastapi>=0.111.0
# Pin Starlette < 1.0 — Starlette 1.0 removed the deprecated
# TemplateResponse(name, context) signature that app.py uses. Lifting this
# pin requires migrating templates.TemplateResponse(...) calls in app.py to
# the new TemplateResponse(request=..., name=..., context=...) form.
starlette>=0.40,<1.0
uvicorn[standard]>=0.29.0
jinja2>=3.1.4
python-multipart>=0.0.9

# Structured LLM extraction
baml-py==0.218.0

# Image processing (HEIC/HEIF support for iPhone photos)
pillow>=10.3.0
pillow-heif>=0.16.0

# Data validation
pydantic>=2.7.0

# API key encryption (session cookies) and download token signing
cryptography>=41.0.0
itsdangerous>=2.2.0

# Environment variable loading
python-dotenv>=1.0.0
